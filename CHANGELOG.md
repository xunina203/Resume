# 版本记录

本文件记录每次重要提交的内容和恢复基线。简历属于高敏感职业资料，后续提交应先更新本文件，再提交和推送。

## 2026-07-19｜将简历与自我介绍展开到主目录

### 本次变更

- 将 `简历/` 下的 4 个文件全部移动到仓库根目录，文件名保持不变。
- 将 `自我介绍/` 及其 `素材/` 下的 4 个文件全部移动到仓库根目录，文件名保持不变。
- 删除清空后的 `简历/`、`自我介绍/` 和 `自我介绍/素材/` 目录。
- 更新 `index.html` 中的简历下载与完整 HTML 简历链接。
- 更新 `README.md` 中的快捷链接、目录结构、内容基线和维护规则。

### 文件安全核对

- 本次只调整 8 个简历及自我介绍文件的路径，不修改文件内容。
- 暂存时 8 个文件应全部识别为 100% 内容一致的重命名。
- 主页中的两个本地链接应改为根目录文件名并可正常访问。

### 恢复信息

- 本次调整前的恢复基线：`5a0d7e0`（展开历史简历并使用点号命名）。
- 本条记录所在提交即为简历与自我介绍完全展开后的恢复点。

## 2026-07-19｜展开历史简历目录

### 本次变更

- 将 `简历/历史版本/Nina的中文简历.pdf` 移至 `简历/Nina的中文简历.历史版本.pdf`。
- 使用点号表示原来的“历史版本”分类，不再保留简历子目录。
- 更新 `README.md` 中的目录结构和维护规则。

### 文件安全核对

- 本次只调整历史简历的路径和文件名，不修改 PDF 内容。
- 暂存时应识别为 100% 内容一致的重命名。

### 恢复信息

- 本次调整前的恢复基线：`d8c3745`（展开各公司求职准备目录）。
- 本条记录所在提交即为历史简历展开后的完整恢复点。

## 2026-07-19｜展开求职准备目录

### 本次变更

- 将 `求职准备/大疆/` 移至仓库根目录 `求职准备-大疆/`。
- 将 `求职准备/联想/` 移至仓库根目录 `求职准备-联想/`。
- 将 `求职准备/施耐德/` 移至仓库根目录 `求职准备-施耐德/`。
- 更新 `README.md` 中的目录结构和维护规则。

### 文件安全核对

- 本次只调整 6 份求职准备文档的路径，不修改文档正文。
- 暂存时应全部识别为 100% 内容一致的重命名。

### 恢复信息

- 本次调整前的恢复基线：`16b58bb`（整理简历资料库并新增个人主页）。
- 本条记录所在提交即为目录展开后的完整恢复点。

## 2026-07-19｜整理资料库并新增个人主页

### 本次变更

- 按用途将仓库整理为 `简历/`、`自我介绍/`、`求职准备/`、`海报/` 和 `工具/`。
- 将旧版简历归入 `简历/历史版本/`，保留当前简历、优化版和历史版本。
- 将大疆、联想、施耐德材料统一归入 `求职准备/` 下的公司目录。
- 新增 GitHub Pages 与本地双用主页：`index.html`、`assets/styles.css`、`assets/main.js` 和 `.nojekyll`。
- 主页内容以 `简历/徐亚楠-物料计划简历-20260718.pdf` 为事实基线，展示核心能力、职业经历、关键成果、教育认证和联系方式。
- 主页新增响应式布局、深浅色切换、打印及简历下载入口。
- 更新 `README.md`，说明仓库结构、主页使用方式、内容基线和维护规则。
- 统一部分 HTML 文件中的中文姓名和英文姓名标题。

### 文件安全核对

- 逐项比较迁移前后的简历、海报、求职准备和工具文件，确认迁移文件内容一致。
- 最新简历 PDF 可正常识别，共 2 页；主页链接指向最新 PDF 和完整 HTML 简历。
- 已检查桌面端、手机端及 320 px 极窄屏，无横向溢出。
- 已验证深浅色切换和页面脚本运行正常。

### 恢复信息

- 本次整理前的恢复基线：`e7e59b0`（新增简历与求职准备材料）。
- 本条记录所在提交即为整理后的完整恢复点。
- 如需整体撤销本次整理，应对本条记录所在提交执行一次反向提交；不要改写或强制覆盖历史。

## 后续提交规范

- 每次提交前先在本文件顶部新增日期、目的、修改文件和验证结果。
- 一个提交只处理一类明确变更，避免把简历正文、网页设计和资料归档混在无关提交中。
- 修改简历事实、数字、任职时间或联系方式时，明确列出旧值和新值。
- 文件移动或重命名时，记录原路径和新路径，并检查内容是否一致。
- 推送前复核暂存内容、提交摘要和远端分支；禁止强制推送。
